export const initialState = {
    loading: false,
    errors: [],
    chuck: ""
};

const chuckReducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state
    };
};

export default chuckReducer;